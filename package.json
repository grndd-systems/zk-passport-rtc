{
  "name": "zk-passport-rtc",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-knowledge passport verification with WebRTC",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist node_modules && rm -rf node_modules"
  },
  "keywords": [
    "zk-passport",
    "webrtc",
    "zero-knowledge",
    "monorepo"
  ],
  "author": "GRNDD Systems",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
